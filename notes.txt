1. Install virualenv
      MAC  python3 -m pip Install virualenv
      WIN - py -m pip install virtualenv
      -- using pyton i want to use modulos

2.  We need to create the virtualenv
    WIN  py -m venv venv
      mac   python 3 -m venv venv

3.  we need to launch  the virtualenv
      mac  source venv/bin/activate
      win   venv\scripts\activate

4.  we need to install flask - this is the server library

  
MAC     python 3 -m venv venv
WIN     py -m pip install flask